/*

Build a program that reads an integer N from the terminal, then reads N doubles from the terminal
and computes the average

Example:
{
    Insert number of elements(N): [Input]
    [Input1]
    [Input2]
    ...
    [InputN]
    The average is: {average}
}

*/