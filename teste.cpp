#include <iostream>
using namespace std;

int main() {
    int x = 1;
    cout << x << endl;

    int parent, child;
    for(int i = 0; i < G.e ; i++) {
        
        //entro kernel mode
        scanf(""....);
        //saiu kernel mode

        //muita logica
    }

    std::vector<int> parentVector = std::vector<int>(G.e);
    int parentArray[G.e], childArray[G.e];
    
        //entro kernel mode
    for(int i = 0; i < G.e ; i++) {
        scanf(""....);
        
    }
    //saiu kernel mode

    for (int i = 0; i < G.e; i++) {
        //muita logica
    }
    return 0;
}

